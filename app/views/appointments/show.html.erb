<h2>Appointment</h2>
<p>href: <%= link_to @appointment.patient.name, patient_path(@appointment.patient_id) %>, your appointment with href: <%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor_id) %> is scheduled for <%= @appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></p>
