<h2>Dr. <%= @doctor.name %> from the <%= @doctor.department %> Department, Your Appointments are:</h2>
<ul>
    <% @doctor.appointments.each do |appt| %>
        <li><%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> for <%= link_to appt.patient.name, patient_path(appt.patient) %></li>
    <% end %>
</ul>

